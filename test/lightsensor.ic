void main()
{
    int sensor = 0;
    while(1) {
        sensor = analog(3);
        printf("s3= %d", sensor);
        if(sensor <=65)
          printf("white\n");
        else
          printf("black\n");
        sleep(0.5);
    }
}
